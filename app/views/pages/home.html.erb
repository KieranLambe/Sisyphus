<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <%= stylesheet_link_tag 'home', media: 'all' %>
</head>
<body>
  <div class="container p-3">
    <!-- Title -->

    <!-- Button container -->
    <div class="button-container">
      <!-- Enclosing div for buttons -->
      <div class="button-wrapper">
        <!-- Journal Button -->
        <a href="#" class="button">Journal</a>
        <!-- Forum Button -->
        <a href="#" class="button">Forum</a>
        <!-- Dashboard Button -->
        <a href="#" class="button">Dashboard</a>
      </div>
    </div>

    <h1 class="title">Sisyphus</h1>
    <h2 class="sub">The legend of you trying <Br> to get stuff
      done
    </h2>
  </div>
  <%if @user_tasks.length > 0%>
    <div class="row">
      <div class="col-md-6"></div>
      <div class="col-md-4 offset-md-2">
        <h2> Your tasks for the day </h2>
        <% @user_tasks.each do |user_task| %>
          <% if user_task.complete == false %>
            <h3><%= link_to user_task.task.title, user_user_task_path(current_user, user_task) %></h3>
            <%= link_to 'Toggle Complete', toggle_complete_user_task_path(user_task), method: :patch, class: 'btn btn-primary' %>
          <% else %>
            <h2><%= user_task.task.title %> This task is complete</h2>
          <% end %>
        <% end %>
      </div>
    </div>
  <%end%>
  <div class="row justify-content-center">
    <div class="col-sm-8 my-3">
      <%= form_with url: tasks_path, method: :get, class: "d-flex" do %>
        <%= text_field_tag :query,
              params[:query],
              class: "form-control",
              placeholder: "Type a keyword"
          %>
        <%= submit_tag "Search", name: "", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
  <div class="container">
    <h2>Tasks</h2>
    <div class="row">
      <% @tasks.each do |task| %>
        <div class="col-md-4 mb-4">
          <div class="card shadow h-100" style="border-radius: 0.5rem;">
            <div class="card-body">
              <h3 class="card-title"><%= task.title %></h3>
              <p class="card-text"><%= task.description %></p>
              <% if task.interests.any? %>
                <p class="card-text"><strong>Interests:</strong> <%= task.interests.map(&:title).join(", ") %></p>
              <% end %>
              <%= link_to 'Add to my tasks', user_tasks_path(task_id: task.id), method: :post, class: 'btn btn-primary float-end' %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <div class="d-flex justify-content-end">
      <%= paginate @tasks%>
    </div>
  </div>
  </body>
</html>
